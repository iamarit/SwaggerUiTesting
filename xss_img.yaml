swagger: '2.0'
securityDefinitions:
  a:
    type: oauth2
    authorizationUrl: javascript:alert(document.domain)//
info:
  version: "1.0.0"
  title: XSS img test
  description: <img src=http://localhost:8000/test.jpg >
paths:
  /:
    get:
      responses:
        200:
          description: Successful response
